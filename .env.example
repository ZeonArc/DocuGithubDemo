# DocuGithub Environment Variables
# Copy this file to .env and fill in your values

# ============================================
# N8N WEBHOOK CONFIGURATION
# ============================================
# For local development:
VITE_N8N_WEBHOOK_BASE=http://localhost:5678/webhook-test
# For production, use your n8n cloud/self-hosted URL:
# VITE_N8N_WEBHOOK_BASE=https://your-n8n-instance.com/webhook

# HMAC Secret for webhook signature validation (must match n8n WEBHOOK_SECRET)
VITE_WEBHOOK_SECRET=your_random_32_character_secret_here

# ============================================
# SUPABASE CONFIGURATION
# ============================================
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ============================================
# AUTH0 CONFIGURATION
# ============================================
VITE_AUTH0_DOMAIN=your-tenant.auth0.com
VITE_AUTH0_CLIENT_ID=your_auth0_client_id
VITE_AUTH0_AUDIENCE=https://your-tenant.auth0.com/api/v2/

# ============================================
# NETLIFY DEPLOYMENT NOTES
# ============================================
# When deploying to Netlify, set these in:
# Site settings > Build & deploy > Environment variables
#
# Required variables for production:
# - VITE_N8N_WEBHOOK_BASE (your production n8n webhook URL)
# - VITE_WEBHOOK_SECRET (same as n8n WEBHOOK_SECRET)
# - VITE_SUPABASE_URL
# - VITE_SUPABASE_ANON_KEY
# - VITE_AUTH0_DOMAIN
# - VITE_AUTH0_CLIENT_ID
# - VITE_AUTH0_AUDIENCE
#
# ============================================
# AUTH0 CONFIGURATION NOTES
# ============================================
# In Auth0 Dashboard, configure these URLs:
#
# Allowed Callback URLs:
#   https://docugithub.netlify.app, http://localhost:5173
#
# Allowed Logout URLs:
#   https://docugithub.netlify.app, http://localhost:5173
#
# Allowed Web Origins:
#   https://docugithub.netlify.app, http://localhost:5173
